# news-aggregator

## Run locally

Use the local Node server instead of Live Server so RSS requests go through the built-in proxy and avoid browser CORS blocks.

```powershell
node server.js
```

Then open `http://127.0.0.1:5500`.
